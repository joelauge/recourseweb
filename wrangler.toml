name = "knowdrive-api"
main = "server/worker.js"
compatibility_date = "2024-04-01"

[vars]
FRONTEND_URL = "http://localhost:5173" # Update this to your GitHub Pages URL for production

# Note: STRIPE_SECRET_KEY should be added via wrangler secrets:
# wrangler secret put STRIPE_SECRET_KEY
